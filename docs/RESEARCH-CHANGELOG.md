# 투자 통합분석실 변경·오류 이력

## v017-20260913-231726-KST

- 요청: 확정한 최대 3페이지 기본 보기를 CPI부터 실제 적용하고 기존 기록보관실에 분석 저장.
- 원인: 이전 research.html은 독립 앱을 연결한 iframe 메뉴만 제공했고, 승인된 3페이지 시안은 브라우저 세션 메모리만 사용했다.
- 변경: 기본 CPI 화면, 공식 확인본, 근거→핵심 연결, 자료 모음, 분석 저장·고유 주소·수정 이력. 기존 macro.html, chatgpt/, records.html 및 자동 수집 코드는 수정하지 않음.
- 구조·소유권·실패 복구: RESEARCH-STANDARD.md 참조. 새 분석은 같은 records 컬렉션에 idea/schemaVer2로 저장. 접근 규칙 변경 없음.
- 저장: 명시적 버튼, 안정적인 문서 ID, 트랜잭션 충돌 검사, 재시도 중복 방지, 로컬 초안과 내려받기. 기존 기록의 종목·별점·검토일·생성일 보존.
- 검증: Node 자동검사 16개 통과. 실제 Firebase 저장 1건 성공. 기존 records.html에서 동일 검증 기록을 검색해 확인. 실제 브라우저에서 3페이지 확장·전체 페이지·복원·새로고침 미저장 초안 보존 확인.
- 실제 검증 기록: 제목에 [기능검증] 표시한 1건을 보관실에 남김. 투자 판단으로 사용하지 않음. 개인 기록 원문·계정 식별자는 이력에 기록하지 않음.
- 확인본 제한: 2026년 8월 BLS 발표 및 2~8월 월별 상승률. 이 확인본은 자동 갱신하지 않으며 원자료 확인일과 이후 공식 발표일을 화면에서 표시. 장기 시계열·공식 일정 자동 갱신은 후속 범위.
- 작업 중 실패와 복구: 기존 로컬 Git에는 origin이 없고 HTTPS helper도 없어 clone 불가. GitHub 연결 도구로 최신 main에서 별도 브랜치를 만들고 변경 파일만 반영. 기존 미리보기 서버가 .mjs MIME을 제공하지 않아 모듈이 실행되지 않음 → JavaScript MIME을 제공하는 별도 로컬 서버로 검증. 이전 로컬 변경 및 자동 수집 결과는 업로드 대상에서 제외.
- 예방 검사: 클라우드 실패/경합/재시도, 기존 필드 보존, 데이터 누락·음수, 시간대, 안전한 텍스트·URL, 열린 페이지 최대 수를 자동 검사.
- 롤백: 이 변경의 화면/스크립트만 이전 커밋으로 복구. records 문서 및 수정 이력은 삭제하지 않음. 기존 독립 프로그램 주소로 계속 접근 가능.
- 배포: 별도 브랜치와 PR에서 검사 후 운영 기본 주소에 반영. 운영 확인 결과는 PR과 작업 결과에서 확인.
# v018-20260914-000355-KST · 전체 대시보드

- 요청: CPI 적용을 바탕으로 전체 대시보드와 공통 3페이지 보기 연결.
- 변경: 신규 독립 대시보드, 8개 보기·검색·분류, 지표/보관실 기록의 공통 상세·출처·명시적 관계, 원출처를 보존하는 브라우저 핵심 정리. 연구실 기본 진입을 대시보드로 변경하고 CPI·기존 프로그램 유지.
- 보존: 기존 record 쓰기 경로·데이터·수집 스케줄 변경 없음. Firebase 어댑터에 전체 기록 읽기 구독만 추가.
- 과정상 실패: 로컬 검증자료 갱신에서 같은 파일의 삭제·추가를 한 패치에 넣어 도구가 거절했고, 대용량 자료를 명령 인수로 전달한 시도는 Windows 길이 제한으로 실패했다. 개별 파일 패치로 복구했다. 데이터 파일은 배포 변경 목록에서 제외한다.
- 검증: 기존 검사 포함 자동 검사 23개 통과. 로컬 브라우저의 8개 보기 전환, 검색·선택 유지, 전체화면 1페이지→원래 3페이지 복귀, 새로고침 복원, 기존 분석 기록 읽기를 확인했다. 390px 화면에서는 활성 페이지만 표시하고 상단에서 접힌 페이지로 돌아갈 수 있음을 확인했다. 운영 반영은 PR 검사·병합·Pages 배포 완료 후 확인한다.
- 복구: 이 버전의 PR을 되돌리면 이전 CPI 기본 진입으로 복원된다. 원본 기록은 수정하지 않았으므로 데이터 이관·역이관이 필요하지 않다.
- 교훈: 기존 자료를 다른 보기로 보여줄 때 수집시각과 관측기간을 구분하고, 같은 시리즈·단위의 검증된 자료만 결합한다.
# v019-20260914-050618-KST · 뉴스레이더 연결

- 요청: 전체 대시보드에 기존 뉴스레이더 연결.
- 확인: 기존 newsUrl의 실제 피드 316건과 필드 구성을 확인했다. RFC 기사 시각과 시간대 없는 수집시각을 구분했다.
- 변경: 뉴스 미보관/보관실 구분, 8개 보기와 3페이지 상세, 원문 링크·발췌·수집 키워드, 지표 사전 기반 뉴스 후보, 명시적 보관과 주소 기반 중복 방지.
- 보존: 수집기·스케줄·기존 편집 기능 변경 없음. 자료를 열거나 화면이 갱신돼도 원본 기록에 쓰지 않는다. 보관 시 기존 문서가 있으면 개인 판단을 덮어쓰지 않는다.
- 과정상 실패: GitHub 대용량 피드 읽기 제한과 브라우저 자동화 도구 초기화 실패가 있었다. 네트워크 접근 승인 후 Windows 기본 HTTPS 클라이언트의 인증 초기화 실패를 확인했고 Node HTTPS로 실제 자료를 읽었다. 브라우저 검증은 별도 Chrome 테스트 세션으로 수행했다. 임시 로컬 서버 미실행 오류는 서버를 다시 시작해 복구했다.
- 검증: 기존 검사 포함 30개 자동 검사 통과. 실제 316건 피드 로딩과 화면 확인, 테스트 저장소로 3페이지·저장 응답 유실 후 재시도·중복 제거·갱신 실패 목록 보존·390px 모바일을 확인했다. 이 검증은 실제 보관실에 기사를 추가하지 않았다. 운영 저장의 새 기록 생성은 사용자의 실제 보관 동작에서 수행된다.
- 복구: 해당 PR을 되돌려 뉴스 기능을 제거할 수 있다. 이미 보관한 기사는 기존 records 형식이므로 유지하며 삭제·이관할 필요가 없다.
- 교훈: 수집된 텍스트와 공식 확인 사실, 기사 발표시각과 수집시각을 같은 상태로 취급하지 않는다.

# v020 · 공통 3페이지 작업공간으로 통합

- 요청: 기존 보기를 없애고 내용·기능을 3페이지 작업공간으로 이동. 뉴스는 목록, 그래프·기사·분석은 세 번째 페이지에서 조작.
- 변경: 기존 research/macro/chatgpt/CPI 진입 주소를 통합 화면으로 연결. 지표 상세의 수치·발표일·정의·해석·관계·수집 이력·비교를 유지. 뉴스 기본 목록, 읽음 표시, 두 기사 나란히 비교, 기사·공식 출처 읽기, AI 요약·심층 질문, 개인 기록·CPI 초안·보관실 원본 작성 기능을 연결.
- 작업 보존: 최대 세 페이지. 세 번째 내용 이력, 전체화면/복귀, 기사·차트·초안별 지속 DOM. 개인 초안과 기존 CPI 초안, 보관실 작성 초안을 복원. 기존 개인 기록과 공용 records는 원래 인증·저장 경로를 사용하며 데이터 이관/복사는 하지 않음. 클릭·읽음은 온라인 저장을 일으키지 않음.
- 기준: 공식 일정·기준선 검증 로직과 시리즈/단위 일치 검사 유지. OHLC 없는 지표의 봉을 생성하지 않음. 시장 지표는 등록된 시세 위젯, 발표형 지표는 원자료 기간/비교를 제공. 국내 지수의 네이버 삽입 제한은 화면에서 명시.
- 개발 중 실패 1: 포털을 문서 흐름에 추가한 뒤 높이를 재면 작업공간이 40px로 축소되어 내용이 잘렸다. 고정 위치 클래스를 먼저 적용한 뒤 측정하여 해결.
- 개발 중 실패 2: 검색 입력의 blur/change가 목록을 다시 만들어 첫 카드 클릭을 소실시켰다. 보기/분류 항목의 change만 처리하도록 제한.
- 개발 중 실패 3: 시험 기사 HTML의 문자 인코딩이 없어 한국어 검사가 실패했다. UTF-8 응답으로 수정하여 기사 본문 표시까지 확인.
- 개발 중 실패 4: 외부 접근이 차단된 테스트 환경에서 실제 연결 검사가 실패했다. 네트워크 권한 승인 후 같은 검사를 다시 실행해 뉴스 422건과 기존 보관실 4건의 연결을 확인.
- 검증: 기존 30개 + 경로/보기/내용 이력 3개 + 기존 분석 모듈 46개, 총 79개 자동 검사 통과. 테스트 저장소의 뉴스 목록→기사→후보 지표→기간/비교→분석 복귀, 두 기사 비교, 모달 없는 AI 작성, 기존 개인 주소·수정, CPI 이전 초안/근거 복원, 전체화면/복귀·390px 화면을 확인. 실제 보관실 원본 작성 폼을 열고 전체화면 복귀 및 새로고침 후 입력 유지 확인. 실제 기록 쓰기와 유료 AI 호출은 수행하지 않음.
- 미검증/제한: Google 본인 재로그인·다른 기기 동기화·실제 AI 생성 품질은 이번 변경의 실검증 대상에서 제외. 모든 언론사 원문과 거래소 위젯 표시를 보장하지 않음. CPI 고정 확인본의 일정 자동 갱신은 기존 제한 유지.
- 배포: 별도 PR에서 자동 검사를 거친 뒤 GitHub Pages 운영 버전을 확인. 버전·실제 서울 시각은 dashboard-core/release.json에 표시.
- 복구: 해당 PR의 코드 변경만 되돌린다. records와 개인 records 문서는 삭제하거나 역이관하지 않는다. 이전 진입 화면은 Git 이력에서 복구 가능.
- 교훈: 화면 개수보다 첫 클릭·문맥·초안·실제 원본 폼의 연속성을 검사한다. 외부 제공처의 삽입 제한과 프로그램 오류를 구별한다.
