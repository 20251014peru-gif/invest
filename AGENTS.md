# 작업 시작과 인계

- 수정 전에 사용자에게 무엇을 어떻게 변경할지 설명한다.
- 투자기록 보관실 개선 작업은 `docs/HOME_HANDOFF.md`를 먼저 읽는다. 기존 앱 안에서 기능과 입력 경로를 통합하고 독립 프로그램을 임의로 삭제하지 않는다.
- 기준 작업 브랜치, 운영 주소와 실제 버전, 변경되지 않은 원본 데이터를 확인한다. 시험 대역 결과를 운영 검증으로 보고하지 않는다.
- 회사·집에서는 각 PC의 Git 복제본으로 이어서 개발한다. 인계 문서와 코드가 같은 커밋에 있도록 관리한다. 채팅의 자동 동기화는 가정하지 않는다.

<!-- BEGIN USER PC-INDEPENDENT-SERVICES POLICY 2026-09-18 -->
# 회사·집·휴대폰 공통 사용 개발 원칙

사용자 요청: 2026-09-18. 기본 목표는 회사 PC와 집 노트북을 꺼도 인터넷이 연결된 휴대폰에서 본인 서비스와 자료를 사용할 수 있게 하는 것이다.

이 원칙은 웹 서비스·업무 도구·자동 수집·개인 서버의 신규 설계와 변경에 적용한다. 단순 로컬 파일 작업이나 개발용 시험 서버까지 클라우드에 배포하라는 뜻은 아니다.

1. **운영 환경의 PC 독립을 기본 요구사항으로 둔다.** 회사 PC, 집 노트북, 특정 브라우저 창이 계속 켜져 있어야 하는 구조를 최종 운영 구조로 채택하지 않는다. 사용자의 명시적인 다른 요청은 우선한다.
2. **기존 클라우드부터 확인한다.** Firebase, Google Cloud, GitHub 등 기존 프로젝트·배포 서비스·실제 이용 주소·버전을 확인한 뒤 재사용한다. 확인 없이 새 서비스나 중복 요금 구조부터 만들지 않는다.
3. **화면·실행·저장·예약 작업을 각각 점검한다.** ‘Firebase를 쓴다’, ‘사이트가 열린다’, ‘배포 성공’만으로 이전 완료라고 판단하지 않는다. 자료 수집, AI, 이미지, 첨부, 알림, 비용 기록, 백업까지 의존성을 확인한다.
4. **운영 화면에서 PC 주소를 호출하지 않는다.** localhost·127.0.0.1·사설 IP·회사 PC 파일 경로는 개발용 또는 명시적으로 설명한 예외에 한정한다. 바로가기·메뉴·환경 설정·저장된 이미지 링크도 확인한다.
5. **클라우드 데이터를 공통 기준본으로 관리한다.** 중요 자료와 사진을 localStorage·IndexedDB·PC 폴더·컨테이너 임시 디스크에만 저장하지 않는다. 이들은 임시 저장·캐시로 사용할 수 있다. 여러 기기의 동시 수정과 저장 실패를 처리한다.
6. **클라우드에서 접수·처리·결과 저장을 완료한다.** 긴 작업은 접수 완료 후 브라우저를 닫거나 기기를 바꿔도 상태와 결과를 찾을 수 있게 한다. 중복 실행·재시도·외부 호출 비용이 불확실한 상태를 다룬다.
7. **기존 기능과 자료를 보존한다.** 자동 캡처·자막·메모·편집·마인드맵·관계 연결 등을 이전 편의를 위해 조용히 제거하거나 수동 기능으로 바꾸지 않는다. 불가한 기능은 근거·대안·남은 검증을 사용자에게 설명한다.
8. **PC 전용 의존성은 구분한다.** Windows GUI, 데스크톱 카카오톡 조작, 증권사 전용 모듈, 로컬 장비는 일반 웹 서버와 다르다. 공식 원격 API 대체 가능성을 먼저 확인하고, 불가하면 독립 실행 환경과 비용·제약을 명시한다. PC가 필요 없다고 허위 보장하지 않는다.
9. **운영 접근과 비용을 서버에서 제한한다.** 본인 인증·자료 소유권·파일 접근·비밀키 보관·사용량 제한을 적용한다. 브라우저 버튼 숨기기나 CORS만으로 보호됐다고 판단하지 않는다. 일반 예산 알림을 절대 과금 상한이라고 설명하지 않는다.
10. **사실·설계·미검증을 구분한다.** 로컬 소스, 배포 상태, 실제 기능 시험을 구분하고 출처와 점검일을 남긴다. 공급자·가격·제약은 공식 자료로 확인한다. 추정 성공이나 과거 보고를 현재 검증 결과로 바꾸지 않는다.
11. **전환은 백업과 복구를 포함한다.** 최신 기준본 확인, 원본 백업, 별도 시험환경, 자료·사진 대조, 최종 차이분 반영, 되돌리기 절차를 준비한다. 기존 서버를 먼저 끄거나 원본 자료를 삭제하지 않는다.
12. **완료 기준은 실제 사용 시험이다.** 회사 PC가 꺼진 상태 또는 해당 PC에 대한 의존을 차단한 격리 환경에서 외부 네트워크·다른 기기로 로그인, 조회, 새 작업, 수정·저장, 이미지, 재접속을 확인한다. 실제 휴대폰 시험을 못 했다면 미검증으로 남긴다. 사용자 PC 종료는 임의로 실행하지 않는다.
13. **최종 안내에 운영 정보를 남긴다.** 공식 사용 주소, 배포 버전, 자료 저장 위치, 예약 작업 실행 위치, 백업·복구 방법, 비용 구조, 남은 PC 의존성과 미검증 항목을 기록한다. 로컬 시험 성공을 운영 완료라고 보고하지 않는다.

개발 시작 시 프로젝트에 적용된 지침과 기존 배포를 확인하고, 개발 완료 시 위 기준의 통과 여부를 짧게 보고한다. 프로젝트를 집 노트북이나 다른 도구로 넘길 때에는 같은 요구사항을 프로젝트 지침과 인계 문서에도 포함한다.
<!-- END USER PC-INDEPENDENT-SERVICES POLICY 2026-09-18 -->
