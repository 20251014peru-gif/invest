# 변경·오류 누적 이력

## BUILD-001 · 첫 지표 보기
- 요청: 큰 글자·파스텔 카드·스파크라인·그래프 우선 상세 페이지·관계·뉴스·개인 기록·전체 검색.
- 확인: 기존 지표 정의와 수집 데이터의 항목 수가 달랐다. 누락을 0으로 대체하지 않고 표시했다. 수집기가 갱신하면 추가 값도 자동 반영한다.
- 변경: 별도 chatgpt 경로, 데이터/차트/기록 모듈 분리, 고정 지표 ID, 뉴스 주소 정규화, 서울 시간 및 미국 서머타임 표시.
- 시도 1: Git 다운로드 도구의 HTTPS 지원 부재로 실패. 공식 저장소 아카이브로 원본을 확보했다. 원격 반영은 GitHub 연결 도구로 수행.
- 시도 2: 초기 상세 이동은 브라우저 전체 이력을 사용해 외부 사이트로 돌아갈 수 있었다. 앱 내부 경로 이력으로 변경.
- 시도 3: 초안 복원과 사용자의 새 입력이 경합할 수 있었다. 입력 시작 여부와 페이지 세대 번호로 오래된 복원을 차단.
- 시도 4: 관련 뉴스 최신순에서 일반 키워드가 포함된 주변 기사가 앞섰다. 제목·등록 키워드·본문의 일치 가중치 순으로 개선. 인과관계 확정은 하지 않는다.
- 시도 5: 저장소의 기존 권한 구조를 확인해 개인 기록의 안전한 분리가 필요했다. 새 기록 연결은 본인 인증·서버 규칙 검증 완료 전 차단했다.
- 검증: 숫자 결측, %p, 0 기준 변화율, 판정/방향 분리, 정의 병합, 이력 기간, KST/DST, 뉴스 중복/검색, 주소/HTML 처리 8개 자동 검사 통과. 브라우저에서 지표→상세, 뉴스 본문 검색, 저장 실패 시 입력 보존·재진입 복원 확인. 390px 화면의 문서와 상세에 가로 넘침 없음, 기본 글자 16px 확인.
- 미검증: Google 본인 로그인 후 실제 온라인 저장·다른 기기 동기화. 실제 iPhone/Android 기기별 검증. 발표 이력 전체 및 원출처 수치 대조.
- 한계: 차트는 원 수집기의 일별 스냅샷이다. 적은 표본으로 상관계수·시장 순환·미래 방향을 만들어내지 않는다. 뉴스 원문 자체의 사실 검증을 완료한 것은 아니다.
- 복구: 이 앱 경로의 이전 GitHub 커밋으로 코드 복원. 개인 기록은 코드와 분리하므로 코드 복원 때 삭제하지 않는다. 권한 복원 시 신규 개인 기록을 다시 공개하는 규칙으로 되돌리지 않는다.
- 교훈: 공용 저장소 설정을 복제하기 전에 실제 권한을 확인한다. 비동기 화면 변경에는 오래된 결과 차단을 둔다. 수집일과 관측일, 상승과 우호 판정을 혼동하지 않는다.

버전과 서울 시각의 원본은 ../release.json. 첫 버전은 달님이 3일 아침 사용 후 완료 여부를 판단한다.

## BUILD-002 · v002-20260908-040232-KST
- 요청: 독립된 카드 경계, 35개 중요도·전체 상황·조건 해석, 작은 뉴스 목록, 기사 요약 및 전체 질문 AI.
- 수정: 나라별 상단 테두리·그림자, 목적별 핵심16/보조14/상황별5와 이유, 필터·정렬, 4영역의 근거/조건부 요약, 뉴스 날짜/제목/분야/매체 표.
- 규칙 요약: 핵심값 결측·오류·주기별 오래된 값은 제외. 자료 기준일을 표시하며 AI 답변과 명시적으로 구분. 중요도는 편집 기준이며 객관적 예측 점수가 아님.
- AI: OpenAI 선택을 확인. 뉴스/전체 질문 창, 동의 시 메모 포함, 근거 선택과 인용 ID 검사, Firebase 서버 구현 준비. 실제 비밀키·인증·서버 연결 전 요청 버튼 비활성. 라이브 호출·요약 품질 검증 미완료.
- 개발 중 오류: 문자열 치환에서 $$가 $로 바뀌어 중요도 버튼이 동작하지 않음. 함수형 치환으로 수정. 실제 브라우저에서 핵심16·보조14 필터 확인.
- 검사: 자동 17개 통과(기존8 + 중요도/요약5 + AI 요청/응답4). 실제 화면 뉴스 분야 필터·선택 기사 AI 창·전체 질문 창 확인.
- 보완 제안: 실업수당/미 하이일드/실질금리, 반도체 실물 시계열. 추천만 표시하며 미수집 값을 지어내지 않음.
- 복구: chatgpt 경로의 v001 커밋을 복원. 수집기와 기존 앱 변경 없음. 개인 데이터 삭제 없음.

## BUILD-003 · v003-20260908-052100-KST
- Google 로그인 및 운영 도메인 연결. 본인 UID 조건의 개인 records 규칙 적용, aiCache/aiUsage 클라이언트 접근 차단. 규칙 7건 검사 성공.
- OpenAI 실제 인증 및 답변 호출 성공. 초기 키 공백/401 및 잔액 부족429를 실제 검사로 구분해 해결.
- 서울 리전에 Node22 AI 함수 배포, 하루30회 시도 상한과 기사 캐시 구현. 무인증/잘못된 토큰 요청401 확인.
- 배포 시 모델 변수 누락을 명시 설정으로 해결. 배포 이미지 보관7일 설정.
- 키 앞뒤 공백 처리 추가, 설정 화면의 'AI 호출 없음' 문구를 실제 전송 안내로 수정.
- 실제 브라우저 본인 호출·캐시 재사용·기록 동기화 검증은 이어서 진행. 공개 서버 무인증 허용이 개인 자료/AI 호출 허용을 뜻하지 않으며 함수 안에서 본인 토큰 검증.


## BUILD-004 · v004-20260908-052800-KST
- 공개 화면 점검에서 초기 자료 로딩 전 설정 버튼 클릭 시 release.version 오류를 재현. 설정 진입 전 준비 여부 검사와 안내 추가.
- v003 Pages 배포 성공. 이전 HTML 캐시로 옛 자산과 최신 버전 문구가 보이는 상황 확인, 새 버전 링크 쿼리로 최신 화면 확인.
- OpenAI 실제 요청 규격으로 근거 ID 포함 JSON 응답 검사 통과(1809토큰). 이는 함수 인증을 포함한 브라우저 종단 검사가 아님.
- Google 계정 선택창에서 자동 조작 시간초과. 본인 계정 로그인 후 AI/기록 동기화·캐시 검사는 미완료.
- 복구: chatgpt만 v003 커밋으로 되돌리며 서버/기록은 유지.
- 검증: 자동검사17개 통과. 로컬 v004 새로 열고 즉시 설정 버튼 클릭 시 오류 없이 표시 확인.
